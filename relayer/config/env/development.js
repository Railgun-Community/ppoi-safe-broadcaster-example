/*
  These are the environment settings for the DEVELOPMENT environment.
  This is the environment run by default with `npm start` if RELAY_ENV is not
  specified.
*/

module.exports = {
  env: "dev"
};
