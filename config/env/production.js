/*
  These are the environment settings for the PRODUCTION environment.
  This is the environment run with `npm start` if RELAY_ENV=production.
  This is the environment run inside the Docker container.
*/

module.exports = {
  env: "prod"
};
