{
  "name": "@railgun-community/lepton-relayer",
  "version": "1.0.1",
  "description": "A Relayer service-provider with JSON RPC interface over IPFS",
  "main": "index.js",
  "scripts": {
    "start": "node relayer.js",
    "test": "export RELAY_ENV=test && nyc --reporter=text mocha --recursive --timeout=15000 test/unit/",
    "coverage:report": "export RELAY_ENV=test && nyc --reporter=html mocha --timeout 15000 --recursive test/unit/"
  },
  "author": "Railgun Contributors",
  "contributors": [
    "Chris Troutner <chris.troutner@gmail.com>"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "@chris.troutner/ipfs": "^2.0.2",
    "@psf/bch-js": "^4.20.13",
    "ipfs-coord": "^6.7.3",
    "jsonrpc-lite": "^2.2.0",
    "public-ip": "^4.0.4"
  },
  "devDependencies": {
    "mocha": "^9.1.3",
    "nyc": "^15.1.0",
    "sinon": "^12.0.1"
  }
}
