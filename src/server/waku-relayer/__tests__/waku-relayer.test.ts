/* globals describe, before, it, beforeEach, afterEach */
import chai from 'chai';
import chaiAsPromised from 'chai-as-promised';
import { WakuRelayer } from '../waku-relayer';

chai.use(chaiAsPromised);
const { expect } = chai;

describe('waku-relayer', () => {
  before(() => {});
}).timeout(10000);
